# Strapi CMS – Configuración para Astro (web)
# Copiar a .env y ajustar según entorno

# URL del Strapi (usada en SSR y build)
STRAPI_URL=http://localhost:1337
# URL pública expuesta al cliente (Astro – debe iniciar con PUBLIC_)
PUBLIC_STRAPI_URL=http://localhost:1337

# Token de API para colecciones protegidas (opcional si permisos públicos)
# Generar en Strapi: Settings > API Tokens > Create new API Token
STRAPI_API_TOKEN=
# Si necesitas exponer token al cliente (no recomendado, solo para lectura pública)
# PUBLIC_STRAPI_API_TOKEN=

# Para desarrollo local, si Strapi no corre, los services harán fallback a /src/data/*
# No es necesario definir token si los permisos públicos están configurados en Strapi:
# Ver strapi/src/index.ts -> bootstrap public permissions
